---
import Chart from "../../components/Chart";
import Layout from "../../layouts/Layout.astro";

export const prerender = false;

const url = Astro.url.pathname;
console.log(url);
let id = url.split("/").at(-1);
---

<Layout title="Croboost">
  <Chart id={id} client:load />
</Layout>
